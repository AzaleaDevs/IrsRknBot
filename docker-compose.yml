services:
  telegram-bot:
    build: .
    container_name: telegram-bot
    env_file: .env
    environment:
      - DB_PATH=/app/data/facturas.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
